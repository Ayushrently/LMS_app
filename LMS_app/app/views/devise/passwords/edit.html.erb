<div class="auth_page lms-course-shell py-4 py-md-5">
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-12 col-md-10 col-lg-7 col-xl-5">
        <section class="auth_card card border-0 shadow-sm">
          <div class="card-body p-4 p-md-5">
            <p class="text-uppercase small fw-semibold text-secondary mb-2">Secure Access</p>
            <h1 class="auth_title h3 mb-2">Reset your password</h1>
            <p class="auth_subtitle text-body-secondary mb-4">Choose a new password for your account.</p>

            <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put, class: "app_form mt-0" }) do |f| %>
              <%= render "devise/shared/error_messages", resource: resource %>
              <%= f.hidden_field :reset_password_token %>

              <div class="mb-4">
                <%= f.label :password, "New password", class: "form-label" %>
                <% if @minimum_password_length %>
                  <small class="text-muted d-block mb-2">Minimum <%= @minimum_password_length %> characters</small>
                <% end %>
                <%= f.password_field :password, autofocus: true, autocomplete: "new-password", class: "form-control" %>
              </div>

              <div class="mb-4">
                <%= f.label :password_confirmation, "Confirm new password", class: "form-label" %>
                <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control" %>
              </div>

              <div class="auth_actions d-grid gap-2">
                <%= f.submit "Update password", class: "btn btn-primary btn-lg" %>
              </div>
            <% end %>

            <div class="auth_links pt-4 mt-4 border-top">
              <%= render "devise/shared/links" %>
            </div>
          </div>
        </section>
      </div>
    </div>
  </div>
</div>
